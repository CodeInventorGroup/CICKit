language: objective-c
osx_image: xcode10.2
xcode_workspace: CIComponentKit
xcode_scheme: CIComponentKit
xcode_sdk: iphonesimulator12.2

before_install:
  - rvm use system
  - brew update
  - sudo gem install cocoapods --pre
  - pod repo update